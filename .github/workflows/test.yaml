name: Java CI

on:
  push:
    branches: [ test ]
  pull_request:
    branches: [ test ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt' # 使用するJDKのディストリビューション
        java-version: '11'    # 使用するJavaのバージョン

    - name: Compile Java file
      run: javac Test.java

    - name: Run Java program
      run: java Test
